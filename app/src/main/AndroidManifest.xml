<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.unicid.aplicativodentista">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--LOGIN USUARIO -->
        <activity
            android:name=".ui.activity.LoginUsuarioActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


                <activity
                    android:name=".ui.activity.PacienteActivity"
                    android:label="@string/title_activity_paciente"
                    android:theme="@style/AppTheme.NoActionBar">
                </activity>

<!--         LISTA PACIENTE -->
         <activity android:name=".ui.activity.ListaPacientes"/>

        <!-- NOVO USUARIO -->
        <activity android:name=".ui.activity.CadastroUsuarioActivity" />

        <!-- NOVO PACIENTE -->
        <!--<activity android:name=".ui.activity.DadosPacienteActivity"/> -->


        <!-- AGENDA -->
        <activity android:name=".ui.activity.AgendaActivity"
            android:label="Agenda"/>

        <!--AGENDAMENTO-->
        <activity android:name=".ui.activity.AgendamentoActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>